# EXAMPLE ONLY — lokalne placeholdery developerskie. NIGDY nie używać w produkcji.
# Sekrety produkcyjne ustawiane są przez `flyctl secrets set ...` (nie w repo).
DATABASE_URL=postgresql://CHANGE_ME:CHANGE_ME@postgres:5432/unionai
REDIS_URL=redis://redis:6379
QDRANT_URL=http://qdrant:6333
NODE_ENV=development
API_PORT=3000
DASHBOARD_PORT=5173
# CORS_ORIGIN — allowlista domen (comma-separated). Pusty = domyślna allowlista K0nsult; '*' = otwarte (NIE zalecane).
# CORS_ORIGIN=https://uni0nai.k0nsult.cloud,https://unionai.grassrootslobbing.pl,https://k0nsult.cloud
