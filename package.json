{
  "name": "unionai",
  "version": "0.3.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "typescript": "^5.3.3",
    "ts-node": "^10.9.1",
    "@types/node": "^20.10.0"
  },
  "scripts": {
    "dev": "docker-compose up",
    "build": "yarn workspaces foreach run build",
    "test": "npm run test --prefix apps/core"
  }
}
